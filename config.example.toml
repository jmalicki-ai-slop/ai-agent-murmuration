# Murmur Configuration Example
#
# Copy this file to ~/.config/murmur/config.toml and customize as needed.
# All settings are optional - Murmur works with sensible defaults.
#
# See docs/configuration.md for complete documentation.

[agent]
# Backend to use for running agents
# Options: "claude", "cursor"
# Default: "claude"
backend = "claude"

# Global default model to use
# Leave empty to use the backend's default model
# Examples:
#   - "claude-opus-4-5-20251101" (highest quality)
#   - "claude-sonnet-4-20250514" (balanced)
#   - "claude-haiku-4-20250514" (fast/cheap)
# Default: (empty - uses backend default)
model = "claude-opus-4-5-20251101"

# Path to the claude executable
# Default: "claude" (searches PATH)
claude_path = "claude"

# Path to the cursor executable (only needed if using cursor backend)
# Default: (empty)
cursor_path = "cursor-agent"

# Per-agent-type configuration overrides
# Each agent type can override the global backend and/or model settings
# This enables intelligent routing based on task complexity and cost optimization

[agent.implement]
# Configuration for implement agents (write production code)
# Use your most capable model here for best code quality
backend = "claude"
model = "claude-opus-4-5-20251101"

[agent.test]
# Configuration for test agents (write and run tests)
# Can use a cheaper backend if you have unlimited quota elsewhere
backend = "cursor"
# model uses cursor's default

[agent.review]
# Configuration for review agents (code review and feedback)
# High-end model recommended for critical review quality
backend = "claude"
model = "claude-opus-4-5-20251101"

[agent.coordinator]
# Configuration for coordinator agents (multi-agent orchestration)
# Good candidate for cheaper backend - orchestration is less complex
backend = "cursor"
# model uses cursor's default

[workflow]
# Workflow automation settings
# Control which steps are automated after agent completion

# Automatically commit uncommitted changes after agent completion
# Default: true
auto_commit = true

# Automatically push branch to remote after agent completion
# Default: true
auto_push = true

# Automatically create pull request after agent completion
# Requires GitHub token with PR write permissions
# Default: true
auto_pr = true

# Automatically re-spawn agent to address review feedback
# This is opt-in due to potential cost implications
# Default: false
auto_review_loop = false

# Example configurations for different use cases:

# ============================================================================
# RECOMMENDED: Quality-Focused with Cost Optimization
# Use Opus for critical tasks (implement, review), cheaper backend for others
# ============================================================================
# [agent]
# backend = "claude"
# model = "claude-opus-4-5-20251101"
# claude_path = "claude"
# cursor_path = "cursor-agent"
#
# [agent.implement]
# backend = "claude"
# model = "claude-opus-4-5-20251101"  # Best model for coding
#
# [agent.test]
# backend = "cursor"  # Cheaper for test generation
#
# [agent.review]
# backend = "claude"
# model = "claude-opus-4-5-20251101"  # High-end for review quality
#
# [agent.coordinator]
# backend = "cursor"  # Unlimited for orchestration tasks

# ============================================================================
# Minimal (use defaults):
# ============================================================================
# [agent]
# (nothing needed - defaults work great)

# ============================================================================
# Claude-Only with Model Tiers:
# ============================================================================
# [agent]
# model = "claude-sonnet-4-20250514"
#
# [agent.implement]
# model = "claude-opus-4-5-20251101"  # Best for code
#
# [agent.review]
# model = "claude-haiku-4-20250514"  # Cheaper for reviews
#
# [agent.test]
# model = "claude-sonnet-4-20250514"  # Balanced for tests

# ============================================================================
# Development (manual PR creation):
# ============================================================================
# [workflow]
# auto_commit = true
# auto_push = false
# auto_pr = false

# ============================================================================
# Full automation:
# ============================================================================
# [workflow]
# auto_commit = true
# auto_push = true
# auto_pr = true
# auto_review_loop = true
